import { StartValidatorDirective } from './start-validator.directive';

describe('StartValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StartValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
